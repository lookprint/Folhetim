<%= form_for(admin_department) do |f| %>
  <% if admin_department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_department.errors.count, "error") %> prohibited this admin_department from being saved:</h2>

      <ul>
      <% admin_department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :num_block %>
    <%= f.number_field :num_block %>
  </div>

  <div class="field">
    <%= f.label :responsible %>
    <%= f.text_field :responsible %>
  </div>

  <div class="field">
    <%= f.label :phone %>
    <%= f.text_field :phone %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
